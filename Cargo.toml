[package]
name = "vtt-rs"
version = "0.1.8"
edition = "2024"
license = "MIT"
authors = ["Geoff Seemueller"]
description = "Library and CLI for streaming microphone input to OpenAI compatible transcription APIs"
repository = "https://github.com/geoffsee/vtt-rs"
readme = "README.md"
keywords = ["transcription", "openai", "whisper", "audio", "cli"]
categories = ["command-line-utilities", "multimedia::audio", "api-bindings"]

[lib]
name = "vtt_rs"
path = "src/lib.rs"

[[bin]]
name = "vtt-rs"
path = "src/main.rs"

[dependencies]
anyhow = "1.0"
byteorder = "1.5"
candle = { version = "0.11", package = "candle-core" }
candle_nn = { version = "0.11", package = "candle-nn" }
candle_transformers = { version = "0.11", package = "candle-transformers" }
cpal = "0.18"
hf-hub = { version = "0.5", features = ["ureq"] }
hound = "3.5"
rand = "0.10"
reqwest = { version = "0.13", default-features = false, features = ["json", "multipart", "rustls"] }
rubato = "5.0"
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "signal", "fs"] }
tokenizers = { version = "0.23", default-features = false, features = ["fancy-regex"] }

[dev-dependencies]
tempfile = "3"
